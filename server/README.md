# Тестовый сервер ACME

Установка зависимостей:
```bash
pip install flask
```

Запуск тестового сервера:
```bash
python3 server.py config/config.json
```

Проверка поиска и деактивации учетной записи:
```bash
python3 server.py config/etc.json
```